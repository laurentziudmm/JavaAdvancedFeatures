package homeWork;

public class ExerciceThree {
    /**
     * 2.Creaza o clasa abstracta Vehicle care are urmatorii membrii private:-Int speed:
     * viteza cu care se deplaseaza vehiculul-Int gear: treapta de vitezaClasa are urmatoarele metode:
     * -Void changeGear(int newGear):
     * metoda ce seteaza membrul privat gear cuparametrul primit  ce reprezinta noua treapta de viteza a vehiculului-Void speedUp(int increment):
     * metoda ce aduca parametrul primit de metoda la viteza curenta a vehiculului-Void applyBrakes(int decrement):
     * metoda ce scade viteza vehiculului cu valoarea primita prin parametrul metodelClasa are o metoda abstracta:-Void printState():
     * metoda care afiseaza starea obiectului
     * Creati 2 clase Truck si Bus care extind  clasa abstracta Vehicle.
     * Ambele clase treebuie sa implementeze metoda abstracta printState().
     * Implementearea sa afiseze un mesaj sugestiv pentru fiecare clasa, de exemplu: “Truck state: ....” Sau “Bus state:....”.
     * Testate codul folosind urmatoarea secventa de cod:
     * Truck  truck = newTruck(); truck.changeGear(2); truck.speedUp(3); truck.applyBrakes(1);
     * truck.printStates(); Bus bus = newBus(); bus.changeGear(1); bus.speedUp(4); bus.applyBrakes(3); bus.printStates();
     */
}
